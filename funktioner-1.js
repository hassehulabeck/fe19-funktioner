function sekvens(num1, num2) {
    let differens = num2 - num1;
    return (num2 + differens);
}

// Vi skickar in argumenten som två variabler.
var x = 4;
var y = 8;

console.log(sekvens(x, y));


/* Prova att skriva om den här koden så att du 
skickar in argumenten i form av en array. */